# V2 Mediflux Requirements

# SYSTEM DEPENDENCIES (must be installed on host system):
# - tesseract-ocr (OCR engine)
# - tesseract-ocr-fra (French language pack)
# 
# Ubuntu/Debian: sudo apt-get install tesseract-ocr tesseract-ocr-fra
# macOS: brew install tesseract tesseract-lang
# Docker: RUN apt-get update && apt-get install -y tesseract-ocr tesseract-ocr-fra

# Core dependencies
requests>=2.31.0
aiohttp>=3.8.0
asyncio

# Database
# sqlite3 is built-in with Python

# Data processing
pandas>=2.0.0

# OCR for document analysis
pytesseract>=0.3.10
Pillow>=10.0.0

# Local AI (TODO: implement when available)
# mixtral-api-client  # Placeholder for local Mixtral integration

# Web framework (optional for API)
fastapi>=0.104.0
uvicorn>=0.24.0
python-multipart>=0.0.6

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0

# Development
python-dotenv>=1.0.0
